'use strict';

angular.module('prosperenceApp')
.controller('QuestionSearchCtrl', function ($scope, $http, socket, Auth) {
  // Retrieve and display a specific question.
  $scope.viewQuestion = function() {
    // $http.get()
  };
});
